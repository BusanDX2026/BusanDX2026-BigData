# M3. 피처 보강 · 튜닝 · 모델 선택
생성: 2026-09-03 18:37

## 1. DEM 수문 파생 (D8 흐름누적 · TWI · HAND)
- DEM 1884x1631, 유효 872,873셀, 픽셀 30m
- D8 방향 결정 완료 (0.4s). 하류 없는 셀(내부함몰·해안) 42
- 흐름누적 완료 (1.0s). max 16,405셀 (= 14.8 km² 집수)
- 수계 임계 597셀(상위0.5%) → 수계 픽셀 4,371, 평균 수계거리 811m
- `twi` 결측 1,406 → 자치구 중앙값
- 격자 결합 완료 (총 1.5s)

방향 검증 (침수흔적 격자에서 점수가 높아야 정상):
  OK  flow_acc_log_s       침수O 0.503 | 침수X 0.403 | +0.100
  OK  twi_s                침수O 0.533 | 침수X 0.394 | +0.138
  OK  dist_stream_m_s      침수O 0.698 | 침수X 0.687 | +0.011

## 2. 피처 보강 효과 (자치구 GroupKFold5, XGBoost 고정설정)
  기존 HAZARD 6        피처 6 | PR-AUC 0.1730 | ROC 0.8249
  ＋수문 3 (총 9)        피처 9 | PR-AUC 0.1874 | ROC 0.8419

→ 수문 피처 추가 효과: PR-AUC **+8.3%**
  채택: 물리적 근거(집수-배수)와 성능 개선이 함께 확인됨.

## 3. 하이퍼파라미터 튜닝 (자치구 GroupKFold5, PR-AUC 기준)
  ※ 랜덤 CV로 튜닝하면 M2에서 확인한 +143~193% 과대평가를 그대로 최적화하게 되므로 반드시 공간 CV로 튜닝
  cfg1 depth=3 n=300 lr=0.05 mcw=5 → PR-AUC 0.1948 | ROC 0.8478 | 4.1s
  cfg2 depth=4 n=400 lr=0.05 mcw=10 → PR-AUC 0.1939 | ROC 0.8461 | 7.2s
  cfg3 depth=5 n=400 lr=0.05 mcw=10 → PR-AUC 0.1905 | ROC 0.8420 | 7.4s
  cfg4 depth=6 n=600 lr=0.03 mcw=20 → PR-AUC 0.1851 | ROC 0.8391 | 11.8s
  cfg5 depth=4 n=800 lr=0.02 mcw=20 → PR-AUC 0.2017 | ROC 0.8481 | 13.8s
  cfg6 depth=3 n=600 lr=0.03 mcw=30 → PR-AUC 0.2014 | ROC 0.8490 | 9.7s

→ 최적 cfg5: {'max_depth': 4, 'n_estimators': 800, 'learning_rate': np.float64(0.02), 'min_child_weight': 20, 'subsample': np.float64(0.7), 'colsample_bytree': np.float64(0.7)}  (PR-AUC 0.2017)
  튜닝 전(cfg3 = M2 설정) 대비 +5.9%

## 4. 최종 모델 선택
| 모델 | PR-AUC | ROC-AUC | Top5% | Top10% | fit(s) |
|---|--:|--:|--:|--:|--:|
| XGBoost(튜닝) | **0.2017** | 0.8481 | 33.5% | 48.5% | 14.9 |
| LogisticRegression | **0.1539** | 0.8259 | 26.9% | 44.8% | 0.3 |

- XGBoost가 로지스틱 대비 PR-AUC +31.1%, 학습비용 50배
- **채택: XGBoost(튜닝)**. 근거 ① 지형-침수 관계가 비선형·임계적(표고 몇 m 이하에서 급증)이라 선형모델이 부적합 ② 다중공선성에 강건(코드리뷰에서 확인된 지형 피처 간 r 0.7~0.9) ③ SHAP으로 격자별 기여도 분해가 가능해 '왜 이 구역인가'를 행정에 설명할 수 있음
- 로지스틱은 **보조 지표**로 유지: 계수 부호로 물리적 타당성을 상시 점검(부호가 뒤집히면 데이터 이상 신호)

- 산출: features_v2.parquet, M3_튜닝결과.csv, M3_최종설정.json, oof_*.npy